# =============================================================================
# MoveX Frontend - Production Environment Configuration
# Copy this to the frontend project before building
# =============================================================================

# Application Environment
VITE_APP_ENV=production
VITE_DEBUG_MODE=false

# API Base URLs (via Nginx API Gateway on port 8080)
# Nginx handles path rewriting to backend services

# System Service (gateway rewrites /api/system/* -> /api/*)
VITE_API_BASE_URL=http://203.205.31.33:8080

# Auth Service (gateway rewrites /api/auth/* -> /api/v1/*)
VITE_AUTH_API_BASE_URL=http://203.205.31.33:8080/api/auth

# OMS Service (gateway rewrites /api/oms/* -> /api/v1/oms/*)
VITE_OMS_API_BASE_URL=http://203.205.31.33:8080/api/oms

# Master Data Service (gateway rewrites /api/master-data/* -> /api/v1/master-data/*)
VITE_MASTERDATA_API_BASE_URL=http://203.205.31.33:8080/api/master-data

# TMS Service (gateway rewrites /api/tms/* -> /api/v1/tms/*)
VITE_TMS_API_BASE_URL=http://203.205.31.33:8080/api/tms

# Prototype Mode disabled in production
VITE_PROTOTYPE_MODE=false
